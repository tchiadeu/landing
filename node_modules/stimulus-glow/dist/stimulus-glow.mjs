import { Controller as n } from "@hotwired/stimulus";
const e = class e extends n {
  initialize() {
    this.move = this.move.bind(this);
  }
  connect() {
    this.overlayTarget.append(this.childTarget.cloneNode(!0)), document.body.addEventListener("pointermove", this.move);
  }
  disconnect() {
    document.body.removeEventListener("pointermove", this.move);
  }
  move(t) {
    const s = t.pageX - this.element.offsetLeft, i = t.pageY - this.element.offsetTop;
    this.element.style.setProperty("--glow-opacity", "1"), this.element.style.setProperty("--glow-x", `${s}px`), this.element.style.setProperty("--glow-y", `${i}px`);
  }
};
e.targets = ["child", "overlay"];
let o = e;
export {
  o as default
};
